"use client";import{useEffect,useState}from"react";import"./goal-projection.css";type Projection={horizonWeeks:8|12;currentWeight?:number;projectedWeightLow?:number;projectedWeightHigh?:number;observedWeeklyRate?:number;targetWeeklyRateLow?:number;targetWeeklyRateHigh?:number;state:string;confidence:string};type Data={points:{date:string;weight:number}[];projections:Projection[];disclaimer:string};
const copy:Record<string,string>={NOT_ENOUGH_DATA:"Keep checking in. Three dated weights are needed before STHENO draws a direction.",ON_TRACK:"Your observed direction currently overlaps the target pace.",FASTER_THAN_TARGET:"Your recent direction is faster than the target range. Recovery and adherence should be reviewed before any change.",SLOWER_THAN_TARGET:"Your recent direction is slower than the target range. Adherence comes first; targets never change silently.",PLATEAU_POSSIBLE:"The recent trend is relatively flat. More complete observations can separate a plateau from normal noise.",RECENT_CHANGE_WAIT:"A recent target change needs more time before another decision."};
export function GoalProjection(){const[data,setData]=useState<Data|null>(null);useEffect(()=>{void fetch("/api/progress/projection",{cache:"no-store"}).then(r=>r.ok?r.json():null).then(setData)},[]);if(!data)return null;return <section className="goal-projection"><header><div><p className="eyebrow">Goal projection</p><h1>Where the trend<br/>may take you.</h1></div><p>Built from your existing assessment and check-ins. No duplicate weigh-ins, no generated “after” image, and no guarantee language.</p></header><div className="projection-grid">{data.projections.map(p=><article key={p.horizonWeeks}><span>{p.horizonWeeks} weeks</span>{p.state==="NOT_ENOUGH_DATA"?<><h2>Not enough data yet</h2><p>{copy[p.state]}</p></>:<><h2>{p.projectedWeightLow}–{p.projectedWeightHigh} lb</h2><p>{copy[p.state]}</p><dl><div><dt>Observed pace</dt><dd>{p.observedWeeklyRate!>0?"+":""}{p.observedWeeklyRate} lb/week</dd></div><div><dt>Target pace</dt><dd>{p.targetWeeklyRateLow} to {p.targetWeeklyRateHigh} lb/week</dd></div></dl></>}<small>{p.confidence} confidence · goal_projection_v1</small></article>)}</div><p className="projection-disclaimer">{data.disclaimer}</p></section>}
