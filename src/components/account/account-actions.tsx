"use client";import {useState} from "react";export function AccountActions(){const [confirmation,setConfirmation]=useState("");const [message,setMessage]=useState("");async function remove(){const r=await fetch("/api/account/delete",{method:"DELETE",headers:{"content-type":"application/json"},body:JSON.stringify({confirmation})});const x=await r.json();setMessage(r.ok?x.status==="deleted"?"Your account and associated data were deleted.":"Your deletion request is queued.":x.error)}return <section className="card-wide"><h2>Your data</h2><p>Download a machine-readable copy of the account data STHENO stores.</p><a className="button secondary" href="/api/account/export">Download my data</a><h2>Delete account</h2><p>This permanently removes your account and associated application data. Billing should be cancelled first.</p><label className="field">Type DELETE MY ACCOUNT<input value={confirmation} onChange={e=>setConfirmation(e.target.value)}/></label><button className="button secondary" disabled={confirmation!=="DELETE MY ACCOUNT"} onClick={remove}>Delete my account</button><p aria-live="polite">{message}</p></section>}
