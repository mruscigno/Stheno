export type CoachDomain="training"|"technique"|"programming"|"nutrition"|"supplements"|"recovery"|"cardio"|"progress"|"action"|"safety"|"out_of_domain";const groups:Record<Exclude<CoachDomain,"out_of_domain">,RegExp>={safety:/\b(chest pain|faint|severe pain|can'?t breathe|injur|diagnos|eating disorder|starv|purge|overdose)\b/i,action:/\b(change|replace|swap|move|shorten|adapt|travel|rest day|equipment|schedule).*(workout|plan|exercise|week)|\b(shorten|adapt|replace)\b/i,supplements:/\b(creatine|supplement|caffeine|electrolyte|pre-workout)\b/i,nutrition:/\b(calorie|macro|protein|carb|fat loss|muscle gain|diet|meal|fiber|hydration)\b/i,recovery:/\b(sleep|recovery|fatigue|sore|stress)\b/i,cardio:/\b(cardio|running|cycling|steps|zone 2|aerobic)\b/i,progress:/\b(progress|plateau|trend|on track|personal record)\b/i,technique:/\b(form|technique|how do i|muscles|cue|mistake)\b/i,programming:/\b(volume|frequency|split|sets per week|deload|periodization)\b/i,training:/\b(train|workout|exercise|strength|hypertrophy|reps|sets|rir)\b/i};export function classifyCoachRequest(text:string):CoachDomain{for(const d of ["safety","action","supplements","nutrition","recovery","cardio","progress","technique","programming","training"]as const)if(groups[d].test(text))return d;return"out_of_domain"}export function safetyResponse(text:string){if(/\b(chest pain|faint|can'?t breathe)\b/i.test(text))return{classification:"stop",copy:"Stop exercising and seek urgent medical care now. STHENO cannot assess these symptoms."};return{classification:"modify",copy:"I can help with a pain-free training modification, but I can’t diagnose or treat an injury. Stop movements that provoke significant symptoms and seek qualified care for severe, sudden, worsening, or persistent symptoms."}}export function actionIntent(text:string){const intent=/travel/i.test(text)?"traveling":/equipment|gym/i.test(text)?"different_equipment":/short/i.test(text)?"short_on_time":/rest day/i.test(text)?"extra_rest_day":/pain|hurt/i.test(text)?"pain":"schedule_changed";return{intent,scope:intent==="traveling"||intent==="different_equipment"?"week":"session_only"}}export const OUT_OF_DOMAIN="Sorry, I can’t help with that. But if you have a fitness, training, nutrition, supplement, recovery, or STHENO plan question, I’m happy to help.";export function answerQuality(text:string){const banned=["great question","fitness is a journey","everyone is different","as an ai"];return{passes:text.length>=20&&text.length<=2200&&!banned.some(x=>text.toLowerCase().includes(x)),banned:banned.filter(x=>text.toLowerCase().includes(x))}}
