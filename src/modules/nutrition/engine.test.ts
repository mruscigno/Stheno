import{describe,expect,it}from"vitest";import{generateNutritionPlan}from"./engine";const base={weightLb:180,goal:"lose_fat" as const,activity:"moderate" as const,trainingDays:3,safetyClassification:"normal"};describe("nutrition engine",()=>{it("is deterministic and range-based",()=>{const a=generateNutritionPlan(base),b=generateNutritionPlan(base);expect(a).toEqual(b);expect(a.nutrition.targetRange.low).toBeLessThan(a.nutrition.targetRange.high)});it("keeps hydration in a plausible baseline range",()=>expect(generateNutritionPlan(base).nutrition.hydrationMl).toBe(2700));it("keeps a conservative energy floor",()=>expect(generateNutritionPlan({...base,weightLb:80}).nutrition.calories).toBeGreaterThanOrEqual(1400));it("blocks safety referrals",()=>expect(()=>generateNutritionPlan({...base,safetyClassification:"refer"})).toThrow("SAFETY_CLEARANCE_REQUIRED"));it("rejects implausible weights",()=>expect(()=>generateNutritionPlan({...base,weightLb:20})).toThrow("WEIGHT_OUT_OF_RANGE"))});
