export type ComparisonEntry = { slug: string; name: string; published: boolean; verifiedAt: string | null; sourceUrls: string[]; bestFor: string | null; training: string | null; nutrition: string | null; adaptation: string | null; platforms: string | null; pricing: string | null; chooseThemIf: string | null; chooseSthenoIf: string | null };
export const comparisons: ComparisonEntry[] = ["fitbod", "future", "ladder", "juggernautai", "betterme"].map((slug) => ({ slug, name: ({fitbod:"Fitbod",future:"Future",ladder:"Ladder",juggernautai:"JuggernautAI",betterme:"BetterMe"} as Record<string,string>)[slug], published: false, verifiedAt: null, sourceUrls: [], bestFor: null, training: null, nutrition: null, adaptation: null, platforms: null, pricing: null, chooseThemIf: null, chooseSthenoIf: null }));
export const publishedComparisons = comparisons.filter((item) => item.published && item.verifiedAt && item.sourceUrls.length);
