import{describe,expect,it}from"vitest";import{articles,pillars}from"./articles";describe("editorial catalog",()=>{it("contains exactly 25 unique required topics",()=>{expect(articles).toHaveLength(25);expect(new Set(articles.map(a=>a.slug)).size).toBe(25)});it("does not self-publish unreviewed drafts",()=>expect(articles.every(a=>a.reviewStatus==="editorial_review")).toBe(true));it("has eight useful hubs",()=>expect(pillars).toHaveLength(8));});
