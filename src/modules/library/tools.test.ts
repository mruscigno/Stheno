import{describe,expect,it}from"vitest";import{calculate,tools}from"./tools";const input={weightKg:80,heightCm:180,age:40,sex:"male",activity:1.4,goal:"maintain",bodyFat:20,loadKg:100,reps:5,barKg:20,sets:3,distanceKm:5,minutes:25,restingHr:60,days:3}as const;describe("library tools",()=>{it("has exactly 20 unique launch tools",()=>expect(new Set(tools.map(x=>x.slug)).size).toBe(20));it("calculates every tool deterministically",()=>tools.forEach(t=>expect(calculate(t.slug,input)).toEqual(calculate(t.slug,input))));it("returns the Epley estimate in public U.S. units",()=>expect(calculate("one-rep-max",input).headline).toBe("Estimated 1RM: 257 lb"));});
