import{describe,expect,it}from"vitest";import{assessMonth,validateMonthlyAction}from"./engine";
const base={goal:"lose_fat",currentWeightLb:180,priorWeightLb:181,workoutAdherence:90,nutritionAdherence:90,trainingPerformance:"stable" as const,energy:4,recovery:4,sleep:4,hunger:3};
describe("monthly review engine",()=>{it("supports a stable no-change result",()=>{const result=assessMonth(base,{calories:2300});expect(result.recommended_actions).toHaveLength(0);expect(result.reason_codes).toContain("PLAN_STABILITY")});it("proposes a conservative deterministic calorie change",()=>{const result=assessMonth({...base,currentWeightLb:181,priorWeightLb:181},{calories:2300});expect(result.recommended_actions[0].afterState.calories).toBe(2150);expect(validateMonthlyAction(result.recommended_actions[0])).toBe(true)});it("lets recovery override body-composition changes",()=>{const result=assessMonth({...base,currentWeightLb:181,priorWeightLb:181,recovery:1,sleep:2,energy:2},{calories:2300});expect(result.recommended_actions[0].type).toBe("deload_recovery")});it("never makes precise visual claims",()=>{expect(JSON.stringify(assessMonth({...base,photoSetCount:2,completePhotoViews:4}))).not.toMatch(/body.?fat|% fat/i)})});
