import{selectExercise}from"./selection";import type{Equipment,ReplacementReason,StructuredDiff,SubstitutionScope,TrainingProfile,Workout,WorkoutConstraints}from"./types";import{productionExerciseLibrary}from"./exercises";
export function getSubstitutions(exerciseSlug:string,profile:TrainingProfile,equipment:Equipment[]=profile.equipment){const original=productionExerciseLibrary.find(e=>e.slug===exerciseSlug);if(!original)throw new Error("EXERCISE_NOT_FOUND");return productionExerciseLibrary.filter(e=>e.slug!==original.slug&&e.pattern===original.pattern&&e.requiredEquipment.every(item=>equipment.includes(item))).map(e=>({exercise:e,score:100-Math.abs(e.fatigueCost-original.fatigueCost)*8-Math.abs(e.repRange[0]-original.repRange[0]),reasonCodes:["MOVEMENT_PATTERN_PRESERVED","MUSCLE_INTENT_PRESERVED","EQUIPMENT_CONFIRMED"]})).sort((a,b)=>b.score-a.score||a.exercise.slug.localeCompare(b.exercise.slug))}
export function proposeExerciseReplacement(workout:Workout,exerciseSlug:string,reason:ReplacementReason,scope:SubstitutionScope,profile:TrainingProfile,constraints:WorkoutConstraints={}){const target=workout.exercises.find(e=>e.exerciseSlug===exerciseSlug);if(!target)throw new Error("PRESCRIPTION_NOT_FOUND");if(reason==="discomfort")return{replacement:null,scope,reason,classification:"modify" as const,reasonCodes:["DISCOMFORT_SAFETY_ROUTE","NO_DIAGNOSIS","CONFIRM_REPLACEMENT"],confirmationRequired:true};const ranked=getSubstitutions(exerciseSlug,profile,constraints.availableEquipment);if(!ranked.length)throw new Error("NO_SAFE_REPLACEMENT");return{replacement:ranked[0].exercise.slug,alternatives:ranked.slice(1,4).map(x=>x.exercise.slug),scope,reason,classification:"normal" as const,reasonCodes:[...ranked[0].reasonCodes,`SCOPE_${scope.toUpperCase()}`],confirmationRequired:scope!=="session_only"}}
export function reconstructWorkout(workout:Workout,constraints:WorkoutConstraints,profile:TrainingProfile):{workout:Workout;diff:StructuredDiff}{const budget=constraints.availableMinutes??profile.sessionMinutes;const equipment=constraints.availableEquipment??profile.equipment;const unavailable=new Set(constraints.unavailableExercises??[]);const kept=[] as Workout["exercises"];const removed:string[]=[];const added:string[]=[];for(const item of workout.exercises){const exercise=productionExerciseLibrary.find(e=>e.slug===item.exerciseSlug)!;const equipmentOkay=exercise.requiredEquipment.every(e=>equipment.includes(e));if(!equipmentOkay||unavailable.has(item.exerciseSlug)){const candidate=selectExercise({profile:{...profile,equipment},pattern:exercise.pattern,role:item.role,usedSlugs:kept.map(k=>k.exerciseSlug),availableEquipment:equipment});kept.push({...item,exerciseSlug:candidate.exercise.slug,exerciseName:candidate.exercise.name,alternatives:candidate.alternatives,reasonCodes:[...candidate.reasonCodes,"WORKOUT_RECONSTRUCTED"]});removed.push(item.exerciseSlug);added.push(candidate.exercise.slug)}else kept.push(item)}while(kept.reduce((sum,e)=>sum+e.estimatedMinutes,5)>budget&&kept.length>2){const dropIndex=kept.map((e,i)=>({i,priority:e.role==="primary"?3:e.role==="secondary"?2:1})).sort((a,b)=>a.priority-b.priority||b.i-a.i)[0].i;removed.push(kept[dropIndex].exerciseSlug);kept.splice(dropIndex,1)}const rebuilt={...workout,estimatedMinutes:kept.reduce((sum,e)=>sum+e.estimatedMinutes,5),exercises:kept,reasonCodes:[...workout.reasonCodes,"CONSTRAINT_RECONSTRUCTION"]};return{workout:rebuilt,diff:{removed,added,preserved:kept.filter(e=>!added.includes(e.exerciseSlug)).map(e=>e.exerciseSlug),reasonCodes:["PRIORITY_STIMULUS_PRESERVED","TIME_BUDGET_HONORED","EQUIPMENT_CONSTRAINTS_HONORED"],confirmationRequired:true}}}
export function proposeScheduleAdaptation(workoutKeys:string[],unavailableOrdinals:number[],availableOrdinals:number[]){const proposed=workoutKeys.map((key,index)=>({workoutKey:key,originalOrdinal:index+1,proposedOrdinal:availableOrdinals[index]??null,status:availableOrdinals[index]?"moved":"conflict"}));return{proposed,conflicts:proposed.filter(x=>x.status==="conflict").map(x=>x.workoutKey),preserved:proposed.filter(x=>x.originalOrdinal===x.proposedOrdinal).map(x=>x.workoutKey),reasonCodes:["ORDER_PRESERVED","FORCED_REST_DAYS_HONORED",unavailableOrdinals.length?"SCHEDULE_DISRUPTION":"NO_CHANGE"],confirmationRequired:proposed.some(x=>x.originalOrdinal!==x.proposedOrdinal)}}
