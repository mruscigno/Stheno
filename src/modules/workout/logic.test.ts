import{describe,expect,it}from"vitest";import{completionSummary,detectPersonalRecords,nextIncompleteSet}from"./logic";import type{Workout}from"@/modules/training/types";
const workout:Workout={key:"day-1",name:"Full Body",dayOrdinal:1,focus:"full_body",estimatedMinutes:45,reasonCodes:[],exercises:[{exerciseSlug:"goblet-squat",exerciseName:"Goblet Squat",role:"primary",sets:2,repMin:6,repMax:10,rir:2,restSeconds:120,estimatedMinutes:10,reasonCodes:[],alternatives:[]}]};
describe("workout execution",()=>{it("finds next set",()=>{expect(nextIncompleteSet(workout,[])).toMatchObject({exerciseIndex:0,setOrdinal:1})});it("calculates honest completion",()=>{expect(completionSummary(workout,[{exerciseSlug:"goblet-squat",setOrdinal:1,load:50,reps:10,rir:2,state:"completed"}],"2026-01-01T00:00:00Z","2026-01-01T00:30:00Z")).toMatchObject({durationMinutes:30,adherencePercent:50})});it("uses deterministic PR definitions",()=>{const prs=detectPersonalRecords([{exerciseSlug:"goblet-squat",setOrdinal:1,load:50,reps:8,rir:2,state:"completed"}],{exerciseSlug:"goblet-squat",setOrdinal:1,load:55,reps:8,rir:2,state:"completed"});expect(prs.map(p=>p.type)).toEqual(["load","reps_at_load","estimated_1rm"])});it("is idempotent by next-set identity",()=>{const logged=[{exerciseSlug:"goblet-squat",setOrdinal:1,load:50,reps:8,rir:2,state:"completed"as const}];expect(nextIncompleteSet(workout,logged)?.setOrdinal).toBe(2)})});
