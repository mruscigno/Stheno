import type{CalendarEvent}from"@/modules/calendar/schedule";
const escape=(value:string)=>value.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
export function localDateAndHour(now:Date,timeZone:string){const parts=new Intl.DateTimeFormat("en-CA",{timeZone,year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",hourCycle:"h23"}).formatToParts(now),part=(type:string)=>parts.find(item=>item.type===type)?.value??"";return{date:`${part("year")}-${part("month")}-${part("day")}`,hour:Number(part("hour"))}}
export function activityReminderEmail(events:CalendarEvent[],appUrl:string){const items=events.map(event=>`<tr><td style="padding:16px 0;border-bottom:1px solid #ded8ca"><strong style="color:#171817">${escape(event.title)}</strong><br><span style="color:#666">${escape(event.detail)}</span></td><td style="padding:16px 0;border-bottom:1px solid #ded8ca;text-align:right"><a href="${appUrl}${event.href}" style="color:#8a682b;font-weight:700">Open</a></td></tr>`).join("");return{subject:events.length===1?`Today: ${events[0].title}`:`${events.length} activities on your STHENO schedule`,html:`<!doctype html><html><body style="margin:0;background:#f2eee4;font-family:Arial,sans-serif;color:#171817"><div style="display:none">Your STHENO schedule for today.</div><main style="max-width:600px;margin:0 auto;padding:40px 24px"><p style="letter-spacing:.18em;color:#8a682b;font-size:12px;font-weight:700">STHENO FITNESS</p><h1 style="font-family:Georgia,serif;font-size:42px;line-height:1.05;margin:16px 0">Your plan is ready for today.</h1><p style="color:#62645f;line-height:1.6">A quick reminder of what is on your schedule. Complete it when it fits your day.</p><table role="presentation" style="width:100%;border-collapse:collapse;margin:24px 0">${items}</table><a href="${appUrl}/app" style="display:inline-block;background:#c9a456;color:#111;padding:14px 20px;border-radius:999px;text-decoration:none;font-weight:700">Open today’s plan</a><p style="margin-top:36px;color:#777;font-size:12px;line-height:1.5">This is an account activity reminder from STHENO Fitness. Manage your account at <a href="${appUrl}/app/account" style="color:#8a682b">STHENO Fitness</a>.</p></main></body></html>`}}
