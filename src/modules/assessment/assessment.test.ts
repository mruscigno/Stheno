import{describe,expect,it}from"vitest";import{questions,validateAnswer}from"./definition";import{interpretFreeContext}from"./interpret";import{buildPersonalizationProfile}from"./profile";import{screenSafety}from"./safety";
describe("Product 2 assessment",()=>{it("keeps definitions versionable",()=>{expect(questions.length).toBeGreaterThan(15);expect(new Set(questions.map(q=>q.key)).size).toBe(questions.length)});it("validates numeric bounds deterministically",()=>{const days=questions.find(q=>q.key==="days_per_week")!;expect(validateAnswer(days,8)).toMatch(/7 or less/);expect(validateAnswer(days,4)).toBeNull()});it("flags urgent text before interpretation",()=>{expect(screenSafety("I fainted and had chest pain").classification).toBe("stop")});it("does not diagnose movement limitations",()=>{const[c]=interpretFreeContext("Squats hurt my knees when traveling");expect(c.type).toBe("temporary_context");expect(c.safetyFlag).toBe(true);expect(JSON.stringify(c)).not.toMatch(/diagnos/i)});it("creates a stable structured handoff",()=>{const out=buildPersonalizationProfile({primary_goal:"build_strength",days_per_week:3,session_minutes:"60",height:70,weight:180,experience:"beginner",recent_consistency:"most_weeks",environment:"gym",activity:"moderate",sleep:"7_8",tracking_comfort:"open"});expect(out.profile.version).toBe("1.0.0");expect(out.profile.availability.daysPerWeek).toBe(3)})});
