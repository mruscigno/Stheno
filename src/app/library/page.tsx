import type{Metadata}from"next";import Link from"next/link";import{pillars}from"@/modules/library/articles";import{tools}from"@/modules/library/tools";export const metadata:Metadata={title:"Free Fitness Library",description:"Evidence-led fitness tools, practical guides, and reviewed exercise education.",alternates:{canonical:"/library"}};export default function Library(){return <main className="public-page shell"><p className="eyebrow">Free STHENO Library</p><h1>Clear answers. Useful tools. No fitness theater.</h1><p className="lede">Start with the question you have today. Every resource explains its limits and points to a practical next step.</p><section className="hub-grid" aria-label="Topics">{pillars.map(p=><Link href={p.slug==="tools"?"/tools":p.slug==="exercises"?"/exercises":`/library/${p.slug}`} key={p.slug}><h2>{p.title}</h2><p>{p.description}</p></Link>)}</section><section><h2>Popular free tools</h2><div className="tool-grid">{tools.slice(0,6).map(t=><Link href={`/tools/${t.slug}`} key={t.slug}><h3>{t.title}</h3><p>{t.summary}</p></Link>)}</div></section></main>}
