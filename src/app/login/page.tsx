import Link from"next/link";import{AuthCard,Field}from"@/components/auth/auth-card";import{signIn}from"@/app/auth/actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const q=await searchParams;return <AuthCard title="Welcome back" subtitle="Sign in to your STHENO account."><form action={signIn}><Field label="Email" name="email" type="email" autoComplete="email"/><Field label="Password" name="password" type="password" autoComplete="current-password"/><p className="form-message" role="status">{q.error??q.message}</p><button className="button" type="submit">Sign in</button></form><p className="muted"><Link href="/forgot-password">Forgot password?</Link> · <Link href="/signup">Create account</Link></p></AuthCard>}
