import type{Metadata}from"next";import Link from"next/link";import{tools}from"@/modules/library/tools";export const metadata:Metadata={title:"Free Fitness Calculators & Tools",description:"Fifteen deterministic fitness tools with practical interpretation and honest limitations.",alternates:{canonical:"/tools"}};export default function Tools(){return <main className="public-page shell"><nav className="breadcrumbs"><Link href="/library">Library</Link> / Tools</nav><p className="eyebrow">15 free tools</p><h1>Calculate less. Understand more.</h1><p className="lede">Every result includes context, limitations, and a useful next step. Calculator inputs stay in your browser.</p><div className="tool-grid">{tools.map(t=><Link href={`/tools/${t.slug}`} key={t.slug}><h2>{t.title}</h2><p>{t.summary}</p><span>Open tool →</span></Link>)}</div></main>}
