import type{Metadata}from"next";import Link from"next/link";import{tools}from"@/modules/library/tools";export const metadata:Metadata={title:"20 Free Fitness Calculators & Tools",description:"Practical calculators for calories, protein, strength, running, body composition, and training plans.",alternates:{canonical:"/tools"}};const groups=[{title:"Calories & nutrition",slugs:["calorie-tdee","macro","protein","weight-loss-timeline","lean-gain-target","hydration-guidance","creatine-guidance"]},{title:"Strength & training",slugs:["one-rep-max","training-max","plate-calculator","training-volume"]},{title:"Running & cardio",slugs:["running-pace","race-time","heart-rate-zones"]},{title:"Body composition",slugs:["bmi-context","lean-body-mass","body-fat-estimate"]},{title:"Planning",slugs:["workout-frequency","training-split","fitness-starting-point"]}] as const;export default function Tools(){return <main className="public-page chrome-shell tools-page"><p className="kicker">Free fitness tools</p><h1>Start with a number.<br/><em>Leave with a plan.</em></h1><p className="lede">Twenty practical tools that explain the result, the limitations, and what to do next. Your inputs stay in your browser.</p><nav className="category-pills">{groups.map(g=><a href={`#${g.title.toLowerCase().replaceAll(" ","-").replace("&","and")}`} key={g.title}>{g.title}</a>)}</nav>{groups.map((g,gi)=><section className="tool-category" id={g.title.toLowerCase().replaceAll(" ","-").replace("&","and")} key={g.title}><div className="category-number">0{gi+1}</div><div><h2>{g.title}</h2><div className="curated-tools">{g.slugs.map(slug=>{const t=tools.find(x=>x.slug===slug)!;return <Link href={`/tools/${slug}`} key={slug}><span>Free tool</span><h3>{t.title}</h3><p>{t.summary}</p><b>Open tool <i>→</i></b></Link>})}</div></div></section>)}</main>}
