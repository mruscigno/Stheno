import{notFound}from"next/navigation";import{configurationStatus}from"@/lib/config/env";export default function Diagnostics(){if(process.env.NODE_ENV==="production")notFound();const status=configurationStatus();return <><p className="eyebrow">Development only</p><h1>Diagnostics</h1><div className="status-grid">{Object.entries(status).map(([key,value])=><div className="status" key={key}><span>{key}</span><strong>{typeof value==="boolean"?(value?"Configured":"Missing"):value}</strong></div>)}</div></>}
