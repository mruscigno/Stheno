import"@/components/brand/member-brand.css";import Image from"next/image";import Link from"next/link";import{signOut}from"@/app/auth/actions";import{ClaimPublicAssessment}from"@/components/assessment/claim-public-assessment";import{createSupabaseServerClient}from"@/lib/supabase/server";import{evaluateMemberAccess}from"@/modules/access/trial";
export default async function AppLayout({children}:{children:React.ReactNode}){const db=await createSupabaseServerClient(),{data:{user}}=db?await db.auth.getUser():{data:{user:null}};let access=null;if(user&&db){const{data:subscription}=await db.from("subscriptions").select("status").eq("user_id",user.id).eq("status","active").limit(1).maybeSingle();access=evaluateMemberAccess({accountCreatedAt:user.created_at,subscriptionStatus:subscription?.status})}return <div className="app-shell"><ClaimPublicAssessment/><aside className="app-nav"><Link href="/app"><Image src="/stheno-logo.png" alt="STHENO Fitness" width={180} height={120} priority/></Link><nav aria-label="Member navigation"><Link href="/app">Today</Link><Link href="/app/coach">Coach</Link><Link href="/app/workout">Workout</Link><Link href="/app/program">Program</Link><Link href="/app/nutrition">Nutrition</Link><Link href="/app/progress">Progress</Link><Link href="/app/checkin">Check-in</Link><Link href="/app/account">Account</Link><form action={signOut}><button className="button secondary" type="submit">Sign out</button></form></nav></aside><main className="app-content">{access?.state==="trial"?<aside className="trial-status" role="status"><span>Free trial</span><strong>{access.daysRemaining} {access.daysRemaining===1?"day":"days"} remaining</strong><Link href="/pricing">View plans</Link></aside>:access?.state==="expired"?<aside className="trial-status expired" role="status"><strong>Trial ended</strong><Link href="/pricing">Choose a plan</Link></aside>:null}{children}</main></div>}
