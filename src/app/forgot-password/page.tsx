import{AuthCard,Field}from"@/components/auth/auth-card";import{requestReset}from"@/app/auth/actions";export default function Forgot(){return <AuthCard title="Reset password" subtitle="We’ll send a secure reset link if the account exists."><form action={requestReset}><Field label="Email" name="email" type="email" autoComplete="email"/><button className="button" type="submit">Send reset link</button></form></AuthCard>}
