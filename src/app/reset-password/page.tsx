import{AuthCard,Field}from"@/components/auth/auth-card";import{updatePassword}from"@/app/auth/actions";export default async function Reset({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <AuthCard title="Choose a new password" subtitle="Use a unique password you do not reuse elsewhere."><form action={updatePassword}><Field label="New password" name="password" type="password" autoComplete="new-password"/><p className="form-message">{q.error}</p><button className="button" type="submit">Update password</button></form></AuthCard>}
