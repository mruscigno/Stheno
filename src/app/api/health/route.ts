import {NextResponse} from "next/server";import {configurationStatus} from "@/lib/config/env";export const dynamic="force-dynamic";export function GET(){const s=configurationStatus();return NextResponse.json({status:s.database?"ok":"degraded",version:s.appVersion,services:{database:s.database,coaching:s.openai,billing:s.stripe,email:s.resend}},{headers:{"cache-control":"no-store"}})}
