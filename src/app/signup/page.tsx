import Link from"next/link";import{AuthCard,Field}from"@/components/auth/auth-card";import{signUp}from"@/app/auth/actions";
export default async function Signup({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <AuthCard title="Create your account" subtitle="Start with a secure STHENO profile."><form action={signUp}><Field label="Email" name="email" type="email" autoComplete="email"/><Field label="Password" name="password" type="password" autoComplete="new-password"/><p className="form-message" role="alert">{q.error}</p><button className="button" type="submit">Create account</button></form><p className="muted">Already a member? <Link href="/login">Sign in</Link></p></AuthCard>}
