import type { Metadata } from "next";
import Link from "next/link";
import { TrackView, TrackedLink } from "@/components/analytics/tracked-link";
import { publicMetadata } from "@/lib/seo";
import { publishedComparisons } from "@/modules/comparisons/catalog";
export const metadata: Metadata = publicMetadata({ title: "Compare Fitness Coaching Options", description: "A fair framework for comparing personalized fitness coaching, training apps, and the support they provide.", path: "/compare" });
const dimensions = [["Training","Is the program personalized once, or adjusted from completed work?"],["Nutrition","Are you given practical guidance, meal structure, or only calorie targets?"],["Adaptation","What happens when travel, recovery, equipment, or schedule changes?"],["Support","Can you ask a question and get a decision you can act on?"],["Ownership","Can you review and export the information you have stored?"]];
export default function CompareHub() { return <main className="compare-page chrome-shell"><TrackView event="comparison_view"/><header><p className="kicker">Compare clearly</p><h1>Choose the kind of fitness support you actually need.</h1><p>Price is only one part of the decision. Compare how a service handles training, nutrition, coaching, changing schedules, equipment, and the amount of decision-making left to you.</p></header><section className="comparison-framework"><h2>A useful comparison starts here.</h2><div>{dimensions.map(([title,copy])=><article key={title}><h3>{title}</h3><p>{copy}</p></article>)}</div></section>{publishedComparisons.length?<section><h2>Verified comparisons</h2>{publishedComparisons.map(item=><Link href={`/compare/${item.slug}`} key={item.slug}>STHENO vs {item.name} →</Link>)}</section>:null}<section className="compare-cta"><h2>See STHENO’s approach using your own starting point.</h2><TrackedLink event="comparison_assessment_click" className="button button-large" href="/assessment">Take the free assessment →</TrackedLink></section></main>; }
